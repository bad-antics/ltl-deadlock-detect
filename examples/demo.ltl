// examples/demo.ltl — runnable demo for ltl_deadlock_detect

import std::io
use ltl_deadlock_detect as lib

fn main() {
    io::println("ltl_deadlock_detect demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}